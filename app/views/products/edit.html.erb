
<section class="ui centered grid container main">

    <section class = "ui grid container">
        <%= form_with(model: @product, url: product_edit_path(@product), local: true, class: "ui form column") do |f| %>
            <%= render 'form', product: @product, image_size: :medium, title: "Service Registration" %>
        <% end %>
    </section>
 
</section>
